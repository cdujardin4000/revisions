<form method="post" action="{{ path('app_project_delete', {'project_id': project.project_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.project_id) }}">
    <button class="btn">Delete</button>
</form>
