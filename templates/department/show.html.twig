{% extends 'base.html.twig' %}

{% block title %}Department{% endblock %}

{% block body %}
    <h1>Department</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Dept_no</th>
                <td>{{ department.deptNo }}</td>
            </tr>
            <tr>
                <th>Dept_name</th>
                <td>{{ department.deptName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ department.description }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ department.address }}</td>
            </tr>
            <tr>
                <th>Roi_url</th>
                <td>{{ department.roiUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_department_index') }}">back to list</a>

    <a href="{{ path('app_department_edit', {'dept_no': department.deptNo}) }}">edit</a>

    {{ include('department/_delete_form.html.twig') }}
{% endblock %}
