{% extends 'base.html.twig' %}

{% block title %}Department index{% endblock %}

{% block body %}
    <h1>Department index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Dept_no</th>
                <th>Dept_name</th>
                <th>Description</th>
                <th>Address</th>
                <th>Roi_url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for department in departments %}
            <tr>
                <td>{{ department.deptNo }}</td>
                <td>{{ department.deptName }}</td>
                <td>{{ department.description }}</td>
                <td>{{ department.address }}</td>
                <td>{{ department.roiUrl }}</td>
                <td>
                    <a href="{{ path('app_department_show', {'dept_no': department.deptNo}) }}">show</a>
                    <a href="{{ path('app_department_edit', {'dept_no': department.deptNo}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_department_new') }}">Create new</a>
{% endblock %}
