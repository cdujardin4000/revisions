{% extends 'base.html.twig' %}

{% block title %}Leave{% endblock %}

{% block body %}
    <h1>Leave</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Leave_id</th>
                <td>{{ leave.leaveId }}</td>
            </tr>
            <tr>
                <th>Emp_no</th>
                <td>{{ leave.empNo }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ leave.type }}</td>
            </tr>
            <tr>
                <th>From_date</th>
                <td>{{ leave.fromDate ? leave.fromDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>To_date</th>
                <td>{{ leave.toDate ? leave.toDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_leave_index') }}">back to list</a>

    <a href="{{ path('app_leave_edit', {'leave_id': leave.LeaveId}) }}">edit</a>

    {{ include('leave/_delete_form.html.twig') }}
{% endblock %}
