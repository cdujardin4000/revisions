<form method="post" action="{{ path('app_leave_delete', {'LeaveId': leave.LeaveId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ leave.LeaveId) }}">
    <button class="btn">Delete</button>
</form>
